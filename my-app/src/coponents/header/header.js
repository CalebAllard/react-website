import React from 'react';
import Nav from './nav';

export default function Header(props){

    return (
        <header>
            <img src="#" alt="logo" />
            <Nav />
        </header>
    );

}